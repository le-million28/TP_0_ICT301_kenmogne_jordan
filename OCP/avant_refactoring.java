public class CalculateurAire {
    public double calculer(Object forme) {
        if (forme instanceof Rectangle) { /* calcul rectangle */ }
        else if (forme instanceof Cercle) { /* calcul cercle */ }
        return 0;
    }
}